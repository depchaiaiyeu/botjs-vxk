{
  "build": {
    "builder": "NIXPACKS",
    "setup": [
      "apt-get update",
      "apt-get install -y python3 python3-pip pkg-config build-essential libcairo2-dev libjpeg-dev libpango1.0-dev libgif-dev librsvg2-dev libpixman-1-dev libpng-dev",
      "ln -sf /usr/bin/python3 /usr/bin/python"
    ]
  },
  "deploy": {
    "startCommand": "npm start"
  }
}
